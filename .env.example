# Optional — without this, state is in-memory only (lost on server restart)
DATABASE_URL=postgresql://user:password@localhost:5432/ipl_auction

# Set to true for local Postgres with SSL (Render sets this via production NODE_ENV)
# DATABASE_SSL=false

PORT=3001
